# Target shops for scraping

Fill this in during **Phase 1** so both of you use the same sources.

| Platform    | Shop name / description | URL (or API base) | Notes |
|------------|--------------------------|-------------------|--------|
| Shopify    | _e.g. Example Store_     |                   |        |
| WooCommerce| _e.g. Example Shop_      |                   |        |

Use these in `.env` as `SHOPIFY_STORE` and `WOOCOMMERCE_URL` (and keys if needed).
